package bs.domain.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import bs.domain.MediaType;

public interface MediaTypeRepository extends JpaRepository<MediaType, String> {

}
